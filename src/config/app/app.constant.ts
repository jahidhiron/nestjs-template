/** Default company display name used when `COMPANY_NAME` is unset. */
export const COMPANY_NAME = 'Nestjs Template';

/** Default HTTP port used when `PORT` is unset. */
export const DEFAULT_PORT = '8080';

/** Default public API base URL used when `API_BASE_URL` is unset. */
export const API_BASE_URL = 'http://localhost:8080';

/** Global route prefix applied to all API endpoints. */
export const API_PREFIX = '';

/** Current API version number used in versioned route paths. */
export const API_VERSION_NUMBER = '1';
