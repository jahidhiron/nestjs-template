/** Language codes for which `I18nLoader` looks up translation files. */
export const SUPPORTED_LANGUAGES = ['en'];

/** Directory names skipped while recursively scanning for locale folders. */
export const IGNORED_DIRS = ['node_modules', '.git', 'dist'];
