/** Options controlling the Swagger description/example and validation message for {@link ValidateSortBy}. */
export type ValidateSortByOptions = {
  description?: string;
  example?: unknown;
  message?: string;
};
