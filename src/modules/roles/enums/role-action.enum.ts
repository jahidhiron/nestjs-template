export enum RoleAction {
  RoleCreated = 'role.created',
  RoleUpdated = 'role.updated',
  RoleDeleted = 'role.deleted',
  RoleRestored = 'role.restored',
  AdminPermissionsSynced = 'role.admin-permissions.synced',
}
